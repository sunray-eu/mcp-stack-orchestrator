# Optional overrides for local MCP infra.
# Copy relevant keys to <stack-root>/.secrets.env
# (runtime files are generated automatically by stack_infra.sh).

# Host filesystem mounts exposed read-only in MCP containers.
MCP_HOST_FS_ROOT=/
MCP_HOST_FS_USERS=/Users

# SurrealDB local runtime.
SURREALDB_ROOT_USER=root
SURREALDB_ROOT_PASS=root
SURREALDB_DEFAULT_NS=mcp
SURREALDB_DEFAULT_DB=workspace
SURREALDB_RPC_PORT=18083
SURREALDB_WS_HOST=127.0.0.1
SURREALIST_CONNECTION_NAME=Local SurrealDB (Docker)

# Neo4j local runtime (optional graph profile).
NEO4J_HOST=127.0.0.1
NEO4J_HTTP_PORT=17474
NEO4J_BOLT_PORT=17687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=testpass
NEO4J_DATABASE=neo4j
NEO4J_READ_ONLY=true
NEO4J_TELEMETRY=false
NEO4J_SCHEMA_SAMPLE_SIZE=100
MCP_NEO4J_VERSION=v1.4.1

# Docs MCP embedding provider (OPENAI recommended).
OPENAI_API_KEY=
DOCS_MCP_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_API_BASE=
GITHUB_TOKEN=

# Alternative docs embedding providers (optional).
GOOGLE_API_KEY=
GOOGLE_APPLICATION_CREDENTIALS=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_API_INSTANCE_NAME=
AZURE_OPENAI_API_DEPLOYMENT_NAME=
AZURE_OPENAI_API_VERSION=
