# Optional per-repository MCP overrides.
# Place as `.mcp-stack.env` in a repository root.
# Parsed as strict KEY=VALUE data (no shell execution).

# -------- Qdrant wrapper (mcpx-qdrant) --------
# workspace | global | manual
MCP_QDRANT_COLLECTION_MODE=workspace
# Used when mode=workspace/global
# MCP_QDRANT_COLLECTION=proj-custom-name
# QDRANT_URL=http://127.0.0.1:6333
# QDRANT_API_KEY=
# QDRANT_LOCAL_PATH=/absolute/path/to/local/qdrant
# EMBEDDING_PROVIDER=fastembed
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
# TOOL_STORE_DESCRIPTION=Store high-signal implementation decisions/snippets with metadata.
# TOOL_FIND_DESCRIPTION=Find relevant snippets/decisions for current coding context.
# FASTMCP_LOG_LEVEL=INFO
# FASTMCP_DEBUG=false

# -------- LSP wrapper (mcpx-lsp) --------
# auto | python | typescript
MCP_LSP_MODE=auto
MCP_LSP_PREFERENCE=typescript
# MCP_LSP_FALLBACK=typescript
# MCP_LANGUAGE_SERVER_BIN=/path/to/mcp-language-server
# MCP_TS_LSP=/opt/homebrew/bin/typescript-language-server
# MCP_PY_LSP=/opt/homebrew/bin/pyright-langserver
# MCP_LSP_LOG_LEVEL=INFO
